app = "border-empires-gateway-staging"
primary_region = "arn"

[build]
  dockerfile = "Dockerfile.gateway"

[env]
  NODE_ENV = "staging"
  HOST = "0.0.0.0"
  PORT = "8080"
  GATEWAY_ALLOW_SEED_FALLBACK = "0"
  SIMULATION_ADDRESS = "border-empires-simulation-staging.flycast:50051"
  SIMULATION_SEED_PROFILE = "season-20ai"
  GATEWAY_SIMULATION_WAKE_ADDRESS = "border-empires-simulation-staging.flycast:50051"
  GATEWAY_SIMULATION_SUBSCRIBE_TIMEOUT_MS = "10000"
  GATEWAY_SIMULATION_PING_TIMEOUT_MS = "10000"
  GATEWAY_SIMULATION_SUBMIT_TIMEOUT_MS = "2500"
  GATEWAY_SIMULATION_HEALTH_FAILURE_THRESHOLD = "8"
  GATEWAY_SIMULATION_WAKE_MAX_ATTEMPTS = "5"
  GATEWAY_SIMULATION_WAKE_BASE_DELAY_MS = "200"
  GATEWAY_SIMULATION_WAKE_MAX_DELAY_MS = "800"
  GATEWAY_SIMULATION_WAKE_TOTAL_TIMEOUT_MS = "20000"
  GATEWAY_DB_APPLY_SCHEMA = "1"
  GATEWAY_DISABLE_FOG = "1"

[http_service]
  internal_port = 8080
  force_https = true
  auto_start_machines = true
  auto_stop_machines = "stop"
  min_machines_running = 0
  processes = ["app"]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory = "256mb"
