app = "border-empires-gateway-staging"
primary_region = "arn"

[build]
  dockerfile = "Dockerfile.gateway"

[env]
  NODE_ENV = "staging"
  HOST = "0.0.0.0"
  PORT = "8080"
  GATEWAY_ALLOW_SEED_FALLBACK = "0"
  SIMULATION_ADDRESS = "border-empires-simulation-staging.internal:50051"
  SIMULATION_SEED_PROFILE = "season-20ai"
  GATEWAY_SIMULATION_WAKE_ADDRESS = "border-empires-simulation-staging.flycast:50051"
  GATEWAY_SIMULATION_SUBSCRIBE_TIMEOUT_MS = "30000"
  GATEWAY_SIMULATION_WAKE_MAX_ATTEMPTS = "20"
  GATEWAY_SIMULATION_WAKE_BASE_DELAY_MS = "500"
  GATEWAY_SIMULATION_WAKE_MAX_DELAY_MS = "5000"
  GATEWAY_SIMULATION_WAKE_TOTAL_TIMEOUT_MS = "90000"
  GATEWAY_DB_APPLY_SCHEMA = "1"

[http_service]
  internal_port = 8080
  force_https = true
  auto_start_machines = true
  auto_stop_machines = "stop"
  min_machines_running = 0
  processes = ["app"]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory = "256mb"
