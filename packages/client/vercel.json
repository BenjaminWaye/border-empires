{
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @border-empires/client build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/rally/new", "destination": "/" },
    { "source": "/r/:code", "destination": "/" }
  ]
}
