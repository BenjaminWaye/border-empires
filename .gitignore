node_modules
**/node_modules
**/dist
snapshots
.DS_Store
.vercel
.localhost-seasons/
.env
.env.*
!.env.example
.codex-worktrees/
.worktrees/

# Per-deploy SHA marker generated by scripts/deploy-staging-all.mjs and
# shipped as a static asset by the client build.
packages/client/public/__build_sha.txt

# Local snapshots of the prod SQLite db pulled by
# `pnpm ops:prod-shape:clone-snapshot` for the prod-shape gate.
# Multi-hundred-MB binary blobs; never commit.
.prod-shape-clones/
.local-data/
.firecrawl/
package-lock.json
packages/storybook/storybook-static/
docs/load-results/

# Internal-only engineering notes (infra/access details, planning docs) —
# not meant for the public repo. Kept locally, excluded from git.
docs/plans/
docs/combined-stack-admin-access.md
docs/agents/topics/staging-access.md
docs/branch-protection-audit-*.md
